# Project: <name>

## Stack
- Backend:
- Frontend:
- DB:
- Infra:

## Key commands
```bash
make dev    # start in dev mode
make test   # run tests
make lint   # lint
```

## Project structure
```
src/
  api/       — routers / controllers
  services/  — business logic
  models/    — DB models / entities
tests/       — test suite
```

## Domain vocabulary
<!-- Terms that mean something specific in this project -->
<!-- Example: "kopecks" = monetary amounts stored as Int in DB -->

## Conventions
<!-- Project-specific rules the model must follow -->
<!-- Example: "All API responses use envelope: {data, error, meta}" -->

## Danger zone
<!-- Operations that require explicit user confirmation before executing -->
<!-- Example: "Ask before any DELETE/TRUNCATE — production DB has live transactions" -->
